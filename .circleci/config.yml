version: 2
jobs:
  build:
    working_directory: ~/idontknowwhatiamdoing
    docker:
      - image: circleci/node:8.10.0
    steps:
      - checkout
      - run:
          name: Change directory to publish
          command: 'cd publish'
      - run:
          name: Install dependencies
          command: npm install
      - run:
          name: Polymer build
          command: polymer build
